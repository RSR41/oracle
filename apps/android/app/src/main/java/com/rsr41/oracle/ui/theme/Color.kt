package com.rsr41.oracle.ui.theme

import androidx.compose.ui.graphics.Color

// Oracle Custom Palette - Warm Beige/Gold Theme
val OracleBeige = Color(0xFFFDFBF7)        // 배경용 베이지
val OracleSurface = Color(0xFFF5F0E6)      // 카드 표면
val OracleGold = Color(0xFFD4AF37)         // 골드 액센트
val OracleGoldDim = Color(0xFFC4A030)      // 어두운 골드
val OracleBrown = Color(0xFF4A4036)        // 다크 브라운 (Primary)
val OracleBrownLight = Color(0xFF8D7B68)   // 라이트 브라운
val OracleError = Color(0xFFBA1A1A)        // 에러
val OracleOutline = Color(0xFFE0D8CC)      // 아웃라인

// 누락된 색상 추가 (Theme.kt에서 사용)
val OracleTerracotta = Color(0xFFB8836B)   // 테라코타 (Secondary)
val OracleSand = Color(0xFFE8DDD4)         // 모래색 (Outline)
val OracleOffWhite = Color(0xFFFAF8F5)     // 오프화이트 (Background)

// Legacy colors kept for compatibility if needed (but mapped to new ones ideally)
val Purple80 = OracleGoldDim
val PurpleGrey80 = OracleBrownLight
val Pink80 = OracleGold

val Purple40 = OracleGold
val PurpleGrey40 = OracleBrown
val Pink40 = OracleGoldDim